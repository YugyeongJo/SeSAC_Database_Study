-- 도서를 이름순으로 검색(오름차순)
SELECT *
FROM book
ORDER BY bookname ASC;

-- 도서를 이름순으로 검색(오름차순)
SELECT *
FROM book
ORDER BY bookname;

-- 도서를 가격순으로 검색(오름차순)
SELECT *
FROM Book
ORDER BY price;

-- 도서를 이름순으로 검색(내림차순)
SELECT *
FROM Book
ORDER BY bookname DESC;

-- 도서를 가격순으로 검색(내림차순)
SELECT *
FROM Book
ORDER BY price DESC;

